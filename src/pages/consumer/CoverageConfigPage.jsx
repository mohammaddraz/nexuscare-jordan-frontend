import PageWrapper from '../../components/layout/PageWrapper';
function CoverageConfigPage() {
  return (
    <PageWrapper title="Coverage Configuration" subtitle="Customize insurance contracts for each family member independently.">
      <div className="card glass-panel p-5 text-center">
        <h4 className="fw-bold">Coverage Config — Coming Soon</h4>
        <p style={{ fontSize: '0.85rem', color: 'var(--color-text-secondary)' }}>Per-member plan customization will be built here.</p>
      </div>
    </PageWrapper>
  );
}
export default CoverageConfigPage;
